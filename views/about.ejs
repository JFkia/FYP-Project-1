<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>About Us • MatchMove</title>

  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background-color: #020617;
      color: #0f172a;
    }

    .about-hero {
      min-height: 60vh;
      padding: 6rem 0 4rem;
      background: radial-gradient(circle at top left, #38bdf8 0, #020617 45%, #0b1120 100%);
      color: #e5f4ff;
      position: relative;
      overflow: hidden;
    }

    .about-hero::before {
      content: "";
      position: absolute;
      inset: 0;
      background: radial-gradient(circle at 80% 20%, rgba(56, 189, 248, 0.18), transparent 55%);
      pointer-events: none;
    }

    .about-hero h1 {
      font-weight: 700;
      letter-spacing: 0.03em;
    }

    .about-hero p {
      max-width: 570px;
      color: #cbd5f5;
    }

    .badge-pill {
      border-radius: 999px;
      font-size: 0.75rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      padding: 0.4rem 0.9rem;
      border: 1px solid rgba(148, 163, 184, 0.6);
      color: #e5e7eb;
      background-color: rgba(15, 23, 42, 0.4);
    }

    .stats-strip {
      margin-top: -3rem;
      position: relative;
      z-index: 2;
    }

    .stats-card {
      background: #020617;
      border-radius: 1rem;
      padding: 1.4rem 1.6rem;
      border: 1px solid rgba(148, 163, 184, 0.45);
      box-shadow: 0 18px 40px rgba(15, 23, 42, 0.5);
      height: 100%;
    }

    .stats-label {
      font-size: 0.78rem;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: #9ca3af;
    }

    .stats-value {
      font-size: 1.8rem;
      font-weight: 700;
      color: #f9fafb;
    }

    .section {
      padding: 4.5rem 0;
      background-color: #020617;
    }

    .section-alt {
      background: radial-gradient(circle at left, rgba(15, 118, 255, 0.08), #020617 65%);
    }

    .section-title {
      font-size: 0.85rem;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: #6b7280;
      margin-bottom: 0.4rem;
    }

    .section-heading {
      font-size: 1.9rem;
      color: #f9fafb;
      font-weight: 650;
      margin-bottom: 1rem;
    }

    .section-text {
      color: #9ca3af;
      font-size: 0.96rem;
      line-height: 1.7;
    }

    .pill-tag {
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
      font-size: 0.75rem;
      padding: 0.3rem 0.7rem;
      border-radius: 999px;
      background-color: rgba(15, 23, 42, 0.8);
      color: #e5e7eb;
      border: 1px solid rgba(148, 163, 184, 0.4);
    }

    .pill-dot {
      width: 8px;
      height: 8px;
      border-radius: 999px;
      background: #38bdf8;
    }

    .feature-card {
      background: rgba(15, 23, 42, 0.85);
      border-radius: 0.9rem;
      padding: 1.2rem 1.2rem 1.1rem;
      border: 1px solid rgba(31, 41, 55, 0.9);
      height: 100%;
    }

    .feature-card h5 {
      font-size: 0.98rem;
      color: #e5e7eb;
      margin-bottom: 0.5rem;
    }

    .feature-card p {
      font-size: 0.86rem;
      color: #9ca3af;
      margin-bottom: 0;
    }

    .partners-strip {
      padding: 2.5rem 0;
      border-top: 1px solid rgba(31, 41, 55, 0.8);
      border-bottom: 1px solid rgba(31, 41, 55, 0.8);
      background: radial-gradient(circle at top, rgba(15, 118, 255, 0.08), #020617 70%);
    }

    .partner-logo {
      height: 30px;
      opacity: 0.8;
      filter: grayscale(0.1);
      transition: opacity 0.2s ease, transform 0.2s ease;
    }

    .partner-logo:hover {
      opacity: 1;
      transform: translateY(-2px);
    }

    .culture-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 1.2rem;
    }

    @media (max-width: 768px) {
      .culture-grid {
        grid-template-columns: 1fr;
      }
    }

    .culture-card {
      background: rgba(15, 23, 42, 0.95);
      border-radius: 1rem;
      padding: 1.4rem 1.5rem;
      border: 1px solid rgba(31, 41, 55, 0.9);
      position: relative;
      overflow: hidden;
    }

    .culture-chip {
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 0.14em;
      color: #9ca3af;
      margin-bottom: 0.4rem;
    }

    .culture-title {
      color: #e5e7eb;
      font-weight: 600;
      margin-bottom: 0.4rem;
    }

    .culture-text {
      font-size: 0.86rem;
      color: #9ca3af;
      margin-bottom: 0;
    }

    .culture-card::after {
      content: "";
      position: absolute;
      right: -40px;
      bottom: -40px;
      width: 120px;
      height: 120px;
      border-radius: 999px;
      background: radial-gradient(circle, rgba(56, 189, 248, 0.14), transparent 60%);
      opacity: 0.8;
      pointer-events: none;
    }

    .careers-section {
      padding: 4rem 0 4.5rem;
      background: radial-gradient(circle at center, rgba(56, 189, 248, 0.12), #020617 60%);
      border-top: 1px solid rgba(31, 41, 55, 0.85);
      border-bottom: 1px solid rgba(31, 41, 55, 0.85);
    }

    .btn-ghost-light {
      border-radius: 999px;
      border: 1px solid rgba(148, 163, 184, 0.7);
      color: #e5e7eb;
      padding: 0.6rem 1.2rem;
      font-size: 0.88rem;
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
      background: rgba(15, 23, 42, 0.8);
    }

    .btn-ghost-light:hover {
      border-color: #38bdf8;
      color: #f9fafb;
      background: rgba(15, 23, 42, 0.9);
    }

    footer {
      padding: 2.5rem 0 1.6rem;
      background: #020617;
      color: #6b7280;
      font-size: 0.82rem;
      border-top: 1px solid rgba(31, 41, 55, 0.9);
    }
  </style>
</head>
<body>
    <%- include("partials/navbar") %>    
    <div class="sidebar-hover-zone"></div>
    <%- include("partials/sidebar") %>

</body>
</html>
