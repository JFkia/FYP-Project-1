<!DOCTYPE html>
<html>
<head>
    <title>Review Delivery</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>

<h2>Review Delivery</h2>

<form action="/exceptions/<%= delivery._id %>/update" method="POST" class="review-form">

    <label>Customer Name</label>
    <input type="text" name="customer" value="<%= delivery.customer %>">

    <label>Recipient Name</label>
    <input type="text" name="recipient" value="<%= delivery.recipient %>">

    <label>Delivery Address</label>
    <input type="text" name="address" value="<%= delivery.address %>">

    <label>Status</label>
    <select name="status">
        <option <%= delivery.status === 'Failed' ? 'selected' : '' %>>Failed</option>
        <option <%= delivery.status === 'Delayed' ? 'selected' : '' %>>Delayed</option>
        <option <%= delivery.status === 'Resolved' ? 'selected' : '' %>>Resolved</option>
    </select>

    <label>Notes</label>
    <textarea name="notes"><%= delivery.notes %></textarea>

    <button type="submit">Update Delivery</button>
</form>

</body>
</html>
