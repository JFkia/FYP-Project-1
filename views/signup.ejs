<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Register • Card Delivery Portal</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            min-height: 100vh;
            margin: 0;
            background: radial-gradient(circle at top right, #4dabf7, #020617 55%);
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            color: #fff;
        }

        .auth-wrapper {
            min-height: calc(100vh - 56px);
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem 1rem;
        }

        .auth-card {
            width: 100%;
            max-width: 960px;
            border-radius: 1.25rem;
            overflow: hidden;
            background: rgba(15, 23, 42, 0.92);
            box-shadow: 0 20px 45px rgba(0, 0, 0, 0.7);
            border: 1px solid rgba(148, 163, 184, 0.5);
        }

        .auth-illustration {
            background: linear-gradient(135deg, #0f172a, #1d4ed8);
            color: #e5f0ff;
        }

        .auth-illustration h2 {
            font-weight: 700;
        }

        .auth-illustration ul {
            padding-left: 1.2rem;
        }

        .form-label {
            font-weight: 500;
            color: #e5e7eb;
        }

        .form-control, .form-select {
            background-color: #020617;
            border-color: #1f2937;
            color: #e5e7eb;
        }

        .form-control:focus,
        .form-select:focus {
            background-color: #020617;
            border-color: #22c55e;
            box-shadow: 0 0 0 0.2rem rgba(34, 197, 94, 0.25);
            color: #f9fafb;
        }

        .btn-success {
            border-radius: 999px;
            font-weight: 600;
            letter-spacing: 0.02em;
        }

        .badge-role {
            background: rgba(15, 23, 42, 0.4);
            border-radius: 999px;
            padding: 0.35rem 0.75rem;
            font-size: 0.75rem;
            border: 1px solid rgba(226, 232, 240, 0.3);
        }

        .small-link {
            font-size: 0.9rem;
        }
    </style>
</head>

<body>
    <%- include("partials/navbar") %>

    <div class="auth-wrapper">
        <div class="auth-card row g-0">
            <!-- Left illustration / explanation -->
            <div class="col-md-5 auth-illustration d-none d-md-flex flex-column justify-content-between p-4">
                <div>
                    <span class="badge-role mb-3">Create your secure workspace</span>
                    <h2 class="mb-3">Join the Card Delivery Portal</h2>
                    <p>Choose the role that best matches your responsibilities:</p>
                    <ul class="small">
                        <li><strong>User:</strong> View your own card delivery status.</li>
                        <li><strong>Admin:</strong> Manage users, vendors, and system settings.</li>
                        <li><strong>Courier:</strong> Update delivery attempts and statuses on the go.</li>
                        <li><strong>Compliance:</strong> Monitor exceptions, audit logs, and SLA breaches.</li>
                    </ul>
                </div>
                <div class="mt-4">
                    <small class="opacity-75">
                        You can request role changes later from an administrator if your responsibilities evolve.
                    </small>
                </div>
            </div>

            <!-- Right: form -->
            <div class="col-md-7 p-4 p-lg-5">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h1 class="h4 mb-0">Create an account</h1>
                    <span class="small text-muted">Card Delivery Portal</span>
                </div>

                <% if (errors && errors.length > 0) { %>
                    <div class="alert alert-danger py-2">
                        <% errors.forEach(err => { %>
                            <div><%= err %></div>
                        <% }) %>
                    </div>
                <% } %>

                <form action="/signup" method="POST" class="mt-3">
                    <div class="mb-3">
                        <label class="form-label" for="name">Full name</label>
                        <input type="text" id="name" name="name"
                               class="form-control" placeholder="Jane Doe" required>
                    </div>

                    <div class="mb-3">
                        <label class="form-label" for="email">Email address</label>
                        <input type="email" id="email" name="email"
                               class="form-control" placeholder="you@company.com" required>
                    </div>

                    <div class="mb-3">
                        <label class="form-label" for="password">Password</label>
                        <input type="password" id="password" name="password"
                               class="form-control" placeholder="Minimum 8 characters" required>
                    </div>

                    <div class="mb-3">
                        <label class="form-label" for="role">Role</label>
                        <select id="role" name="role" class="form-select">
                            <option value="user">User</option>
                            <option value="admin">Admin</option>
                            <option value="courier">Courier</option>
                            <option value="compliance">Compliance</option>
                        </select>
                    </div>

                    <button type="submit" class="btn btn-success w-100 mt-2">
                        Create account
                    </button>

                    <p class="small-link text-center mt-3 text-muted">
                        Already have an account?
                        <a href="/login" class="text-info text-decoration-none">Sign in instead</a>
                    </p>
                </form>
            </div>
        </div>
    </div>

    <%- include("partials/footer") %>
</body>
</html>
